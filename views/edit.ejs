<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Add your head content here -->
    <title>Edit Blog Post</title>
    <style>
        body {
            padding: 20px;
            background-size: cover;
            font-family: Arial, Helvetica, sans-serif;
            color: #4B5B4C;
            background-color: #ecf6ec;
        }
        
        body.card main {
            background-size: cover;
            padding-bottom: 40px;
            border: 5px solid #4B5B4C;
        }
        
        main {
            margin: 0;
            border-radius: 10px;
            padding: 20px;
        }
        
        main h1 {
            font-weight: normal;
            text-align: center;
            margin-bottom: 20px;
        }
        
        main .wrapper {
            margin: 10px;
        }
        
        main .wrapper label {
            display: block;
            font-size: 20px;
            margin-bottom: 10px;
        }
        
        main .wrapper input[type="text"] {
            width: calc(100% - 20px);
            padding: 10px;
            font-size: 16px;
            border: 1px solid #9EB09F;
            border-radius: 5px;
        }
        
        main .submission {
            text-align: center;
            margin: 20px;
        }
        
        main .submission input[type="submit"] {
            border: 2px solid #4B5B4C;
            background-color: #9EB09F;
            font-size: 24px;
            padding: 10px 20px;
            border-radius: 10px;
            cursor: pointer;
        }
        
        main .submission input[type="submit"]:hover {
            background-color: #7f8c7d;
        }
        
        .error {
            font-size: 12px;
            color: #9f0b0a;
            text-align: right;
        }
        </style>
        
</head>

<body>
    <h1>Edit Blog Post</h1>
    <form action="/edit/<%= blog._id %>" method="POST">
        <input type="text" name="Name" value="<%= blog.Name %>">
        <input type="email" name="Email" value="<%= blog.Email %>">
        <textarea name="Description"><%= blog.Description %></textarea>
        <button type="submit">Save</button>
    </form>
    
    </form>
</body>

</html>
