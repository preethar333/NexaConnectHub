<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Crowdfunding Post</title>
    <style>
        body {
            background-color: #f5f5f5;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #4B5B4C;
        }

        h1 {
            text-align: center;
            color: #4B5B4C;
        }

        form {
            max-width: 600px;
            margin: 0 auto;
            box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.2);
            padding: 20px;
        }

        .wrapper {
            margin-bottom: 20px;
        }

        label {
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
        }

        input,
        select,
        textarea {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
            margin-bottom: 10px;
        }

        .error {
            color: red;
            margin-top: -10px;
            margin-bottom: 10px;
        }

        .submission {
            text-align: center;
        }
    </style>
</head>
<body>
    <h1>Edit Crowdfunding Post</h1>

    <% if (crowdfundingPost) { %>
        <form action="/edit-crowdfunding/<%= crowdfundingPost._id %>" method="POST">
            <div class="wrapper">
                <label for="Name">Name:</label>
                <input type="text" name="Name" value="<%= crowdfundingPost.Name %>" required>
            </div>
            <div class="wrapper">
                <label for="Email">Email:</label>
                <input type="email" name="Email" value="<%= crowdfundingPost.Email %>" required>
            </div>
            <div class="wrapper">
                <label for="crowdfundingType">Crowdfunding Type:</label>
                <input type="text" name="crowdfundingType" value="<%= crowdfundingPost.crowdfundingType %>" required>
            </div>
            <div class="wrapper">
                <label for="Description">Description:</label>
                <textarea name="Description" rows="4" required><%= crowdfundingPost.Description %></textarea>
            </div>
            <div class="submission">
                <button type="submit">Save Changes</button>
            </div>
        </form>
        
    <% } else { %>
        <p>Crowdfunding post not found.</p>
    <% } %>

    <a href="/crowdfunding">Back to Crowdfunding</a>
</body>
</html>